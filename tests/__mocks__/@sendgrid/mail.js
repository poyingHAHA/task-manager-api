module.exports = {
    setApiKey(){

    },
    send(){

    }
}

// All these is going to do is allow these functions to be called when testing. But in reality, nothing is happening behind the scenes
// and that's good in the test environment, no API keys will be set and no emails will get sent.